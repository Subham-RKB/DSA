#include <bits/stdc++.h>
using namespace std;
int main()
{
    string a;
    unordered_map<char,set<string>> m;
    cin>>a;
    for(int i=0;i<a.length()-1;i+3){
        if(m[a[i]].find((a[i+1]+a[i+2]))!=m[a[i]].find()){
            
        }
    }
}